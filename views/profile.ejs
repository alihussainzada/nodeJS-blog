<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>User Profile - KoalaSEC</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />
  <style>
    body {
      background: #f0f2f5;
    }
    .profile-card {
      max-width: 500px;
      margin: 80px auto;
      border-radius: 15px;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
      background: white;
    }
    .card-header {
      background-color: #007bff;
      color: white;
      border-radius: 15px 15px 0 0;
    }
    .profile-avatar {
      width: 150px;
      height: 150px;
      margin: 0 auto 20px;
      overflow: hidden;
      border-radius: 50%;
      display: flex;
      justify-content: center;
      align-items: center;
      background-color: #ddd;
    }
    .profile-avatar img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      cursor: pointer;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="card profile-card">
      <div class="card-header text-center">
        <h3><%= user.name %>’s Profile</h3>
      </div>
      <div class="card-body text-center">
        <div class="profile-avatar">
          <img
            id="profilePicPreview"
            src="<%= user && user.profilePicture && user.profilePicture.trim() !== '' 
                  ? '/static/images/' + user.profilePicture 
                  : '/static/images/1.png' %>"
            alt="Profile Picture"
          />
        </div>
        <p><strong>Full Name:</strong> <%= user.name %> <%= user.lastname %></p>
        <p><strong>Email:</strong> <%= user.email %></p>

        <div class="d-grid gap-2 mt-4">
          <a href="/posts/create" class="btn btn-primary">Add New Blog Post</a>
          <a href="/user/edit-profile" class="btn btn-warning">Edit Profile</a>
          <a href="/auth/logout" class="btn btn-outline-danger">Logout</a>
        </div>
      </div>
    </div>
  </div>
</body>
</html>
